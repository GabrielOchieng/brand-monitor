# {$API_DOMAIN} must be a real domain/subdomain with its DNS A record pointed at this VM's
# public IP -- Caddy uses it both to know what to serve and to automatically request/renew
# a Let's Encrypt certificate for it. No manual cert handling needed.
{$API_DOMAIN} {
	reverse_proxy api:4000
}
